package main;

import "osif";

declare "C" from "stdio.h" {
    int printf(const char* format, ...);
}

/**
 * 程序入口
 *
 * @return 退出码，0 表示成功
 */
public int32 main() {
    printf("os run!\n");

    osif_os_run();

    return 0;
}
